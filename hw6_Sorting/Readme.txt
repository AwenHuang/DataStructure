quicksort:
	選定一個值(第一個)，為基準，大於他的放右邊，小於他的放左邊，最後在把基準點和比他小的最後一個字母做swap，如此一來基準點的左邊就都是比他小的值，右邊都是比他大的值，接著再利用遞迴的方式去做基準點左右兩邊的陣列直到做完sort為止


mergesort:
	把陣列分割成最小，然後再逐一做合併，邊合併邊比較，利用遞迴的方式去做

heapsort：
	先把陣列做maxheap，接著把heap陣列的第一個(最大值)，存入結果陣列內，存完後把heap陣列的最後一個放去第一個，然後再做maxheap，再繼續做heapsort，直到sort完為止
