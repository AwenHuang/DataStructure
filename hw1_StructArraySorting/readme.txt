先宣告結構，把所需的資料(學生座號、姓名、數學成績、中文成績)設定好
然後宣告一個array of stucture
用argv做讀檔，把測資一行一行用while抓進input的陣列裡面
接著用strtok對input[]做切割的動作(把空白&換行切掉)
用一個指標陣列*store[]去儲存切割完的個別資料
切完的個別資料再分別複製到結構內的個別資料內
最後依照使用者所選的動作做sorting就好了
